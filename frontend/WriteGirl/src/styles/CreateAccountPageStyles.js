import { StyleSheet } from 'react-native';


const styles = StyleSheet.create({

input: {
    backgroundColor: '#FFFFFF',
    borderColor: '#000',
    borderRadius: 3,
    height: 40,
    padding: 10,
    margin: 10
},

label: {
    color: '#000'
}

});

export { styles };
